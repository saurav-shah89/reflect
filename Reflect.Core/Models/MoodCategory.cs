namespace Reflect.Models;

/// <summary>
/// The three groups every mood belongs to. Dashboard analytics report the
/// distribution of entries across these categories.
/// </summary>
public enum MoodCategory
{
    Positive = 0,
    Neutral = 1,
    Negative = 2
}
